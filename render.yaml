services:
    - type: web
      name: flask-backend
      env: python
      plan: free
      buildCommand: "pip install -r requirements.txt"
      start: python backend/app.py
      envVars:
          - key: PYTHON_VERSION
            value: 3.11.3 # Or 3.10
